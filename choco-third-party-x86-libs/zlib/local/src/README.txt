Buiding static zlib on Windows 10 x64
  * Open the zlib Visual Studio solution in build\
  * Make sure the build configuration is Release x64.
  * Open the zlibstatic project's Property Pages
    * In Code Generation -> Runtime Library, changing the /MD to a /MT